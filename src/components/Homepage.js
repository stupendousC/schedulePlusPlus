import React from 'react';
import logo from '../SPPicon.png';

export default function Homepage() {

  return (
    <section>
      <img src={logo} alt="logo" className="homepage-logo"/>
    </section>
  );
}